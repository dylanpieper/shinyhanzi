% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hanzi_db.R
\name{hanzi_db}
\alias{hanzi_db}
\title{Open (or return the cached) read-only DuckDB connection}
\usage{
hanzi_db(path = NULL, read_only = TRUE)
}
\arguments{
\item{path}{Optional explicit path to the \code{.duckdb} file. When supplied the
package cache is bypassed (used internally by \code{build_hanzi_db()}).}

\item{read_only}{Logical; defaults to \code{TRUE}.}
}
\value{
A \code{DBIConnection} object.
}
\description{
Returns the package-level cached connection if already open, otherwise
resolves the database path (downloading if necessary), opens a read-only
connection, loads the FTS extension, and caches it in \code{pkg_env}.
}
